apiVersion: v1
kind: Service
metadata:
  name: poc-service
  namespace: default
spec:
  selector:
    app: poc-python
  ports:
    - protocol: TCP
      port: 80
      targetPort: 8000
  type: ClusterIP
